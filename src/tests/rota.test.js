const Rota = require('../backend/Rota');

describe("rota test case", () => {
    let rota;
    
    beforeEach(() => {
      rota = new Rota();

    });
  
    test('should not generate a bus', () => {
    });
    
  });
  